-> qual serão os dados de saída do método implementado?
    - mostrar de quais pilhas foram retirados os minérios?
    - mostrar qualidade final do pedido realizado?
    - mostrar quantidade retirada de cada pilha?

#issues
    -> qualidade limitante está sendo avaliada em cada pilha, e não apenas para
    a mistura final
        - avaliar alguma forma de permitir que a pilha seja escolhida, mesmo
        não atendendo os parâmetros de qualidade, para que a mistura de várias
        pilhas atendam os critérios do pedido

    -> em todas as instâncias de teste o 4o parâmetro inicial de qualidade de
    cada pilha é 1, e o limite superior de todos os pedidos é 0.5
        - considerar inputs para "diluir" o valor inicial da pilha